<Window x:Class="Kosha.CustomerManager.WindowsClient.Models.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kosha.CustomerManager.WindowsClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="835" Width="1280">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="435" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <!-- Side Menu -->
        <Grid Grid.Column="0" Background="{StaticResource AccentDark}">
            <!-- Side menu content goes here -->
        </Grid>
        <!-- Task List -->
        <Grid Grid.Column="1" Background="{StaticResource TaskListBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="120" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <!-- Task List Header -->
            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="20 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Style="{StaticResource HeadingOne}" Content="Tasks" VerticalAlignment="Center"  />
                    <Button Padding="10" Grid.Column="1" Width="40" Height="40" Style="{StaticResource RoundAccentButton}" >
                       <Viewbox>
                           <Path Data="{StaticResource Refresh}" Fill="#FFFFFFFF" />
                       </Viewbox>
                    </Button>
                </Grid>
                <Grid Grid.Row="1" Height="36" Margin="20 0" >
                    <TextBox Grid.Column="0" Style="{StaticResource SearchBox}"  />
                    <Button Grid.Column="0" Padding="10" 
                            Background="#FFFFFFFF" BorderBrush="Transparent" 
                            HorizontalAlignment="Right" VerticalAlignment="Center"
                            Style="{StaticResource SearchButton}" Margin="0 0 20 0">
                        <Viewbox Width="16" Height="16">
                            <Path Data="{StaticResource Search}" Fill="{StaticResource Borders}" />
                        </Viewbox>
                    </Button> 
                </Grid>
            </Grid>
            <!-- Task List Items -->
            <Grid Grid.Row="1"></Grid>
        </Grid>
        <!-- Main Content Area -->
        <Grid Grid.Column="2" Background="{StaticResource MainContentBackground}">
            <!-- Main content goes here -->
        </Grid>
    </Grid>
</Window>
